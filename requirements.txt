# Pinned to the versions this was built and tested against.
docling==2.122.0
docling-core==2.92.0

# For an NVIDIA GPU, install a CUDA build of torch BEFORE these, e.g.
#   uv pip install torch --index-url https://download.pytorch.org/whl/cu124
# Otherwise pip resolves the CPU-only wheel and --device cuda will fail.
